---
name: backlog
description: Display the current prioritized backlog
---

# Show Backlog

Read and display `TASKS_DIR/BACKLOG.md` to the user.

That's it — just show what's there. Don't regenerate or rescan tickets.

If the user wants to regenerate the backlog from current ticket state, they'll say "clean up the backlog" or "regenerate the backlog" which triggers the backlog-cleanup skill.

Arguments: $ARGUMENTS
