# launch-planning pack (future, disabled)

MVP scoping (`/mvp`, `mvp-agent`) and execution-lane planning (`/mvp-execute`, `mvp-execute`),
copied unchanged from the donor install as a future optional pack.

Shipped **disabled** — `base/profiles.json` marks this profile `"enabled": false`, so nothing here
installs until the pack is finished.

Pending before it ships: consolidating `/mvp` and `/mvp-execute` into one command surface, plus a
token pass. Deferred deliberately; do not enable in a profile until that lands.
